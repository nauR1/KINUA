name: Validate KINUA
on: [push, pull_request]
jobs:
  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test-only-password
          POSTGRES_DB: biometria_test
        ports: ['5432:5432']
        options: --health-cmd "pg_isready -U test -d biometria_test" --health-interval 5s --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r backend/requirements.txt && pip install ruff==0.13.2
      - run: sudo apt-get update && sudo apt-get install -y libgl1 libglib2.0-0 libportaudio2
      - run: ruff check . --diff
        working-directory: backend
      - run: python -m pytest -q tests/test_report_polish.py tests/test_demo.py -k "pdf or report_presentation"
        working-directory: backend
        env:
          TEST_DATABASE_URL: postgresql+psycopg://test:test-only-password@127.0.0.1:5432/biometria_test
          KINUA_REPORT_ARTIFACT_DIR: test-output
      - run: python -m pytest -q
        working-directory: backend
        env:
          TEST_DATABASE_URL: postgresql+psycopg://test:test-only-password@127.0.0.1:5432/biometria_test
      - uses: actions/upload-artifact@v4
        with:
          name: kinua-pdf-validation
          path: backend/test-output/KINUA_Paciente_Teste_ROM.pdf
          if-no-files-found: error
      - run: python -m alembic upgrade head && python -m alembic check && python -m alembic downgrade base && python -m alembic upgrade head
        working-directory: backend
        env:
          DATABASE_URL: postgresql+psycopg://test:test-only-password@127.0.0.1:5432/biometria_test
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
        working-directory: frontend
      - run: npm run prepare:vision && npm run typecheck && npm run lint && npm test && npm run build
        working-directory: frontend
